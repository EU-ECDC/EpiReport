len_history <- calc_len_history(x = x, disease = disease, 
                                year = year, reportParameters = parameters_table,
                                MSCode = EpiReport::MSCode, 
                                index = 1, doc)
